// determine is the second array is a subsequence of the first
// a subsequence array only need to be in the same order of the 1st array
// input: two non-empty arrays
// output: boolean
function isValidSubsequence(array, sequence) {
    let 

}


let array = [5, 1, 22, 25, 6, -1, 8, 10]
let seq = [1, 6, -1, 10]

console.log(isValidSubsequence(array, seq))
